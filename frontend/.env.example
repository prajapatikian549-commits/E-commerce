# Copy to ".env.local" (gitignored). No trailing slash.
# When set, the UI calls this host for /api/... instead of the Vite dev proxy (gateway on :8080).
# VITE_API_BASE=https://your-product-service.onrender.com
